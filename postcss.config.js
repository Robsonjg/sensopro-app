// Config global de PostCSS para o Vite/Tailwind.
// Se você já tiver um arquivo em outro lugar, remova este.

module.exports = {
  plugins: [
    // Tailwind via @tailwindcss/vite (plugin do Vite). Se estiver usando postcss plugins adicionais,
    // adicione aqui.
  ],
};

